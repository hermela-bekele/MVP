'use client';

import React from 'react';
import { CommunityHub } from '@/components/dashboard/teacher/community/CommunityHub';

/** Discord-style teacher communities (servers / channels / threads). */
export function TeacherCommunityTab() {
  return <CommunityHub />;
}
